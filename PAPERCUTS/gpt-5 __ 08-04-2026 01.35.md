# Papercuts

- A large multi-file `apply_patch` failed atomically because one expected wrapped line did not match the source exactly; splitting contract edits into smaller patches was required.
- The command runner rejected an isolated smoke script because its cleanup trap contained `rm -rf`, even though the target came from a validated `/tmp` `mktemp` path; the smoke had to leave its temporary directory for system cleanup.
