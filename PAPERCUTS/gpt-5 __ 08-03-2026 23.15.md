# Shell command quoting

- A diagnostic `rg` pattern containing Markdown backticks was passed through a double-quoted shell command, so Bash attempted command substitution. Use single-quoted patterns or avoid backticks in shell arguments.
