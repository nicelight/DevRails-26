# Papercuts

- A broad `rg` search under `~/.config` descended into archived Codex session logs and produced heavily truncated output. Exclude runtime-history and browser-profile directories, or restrict the search to known KDE configuration paths.
