# Papercut

- Firefox exposes a Marionette listener on `127.0.0.1:2828`, but a direct protocol probe only returned the greeting and did not complete session creation; process-level inspection remained usable.
