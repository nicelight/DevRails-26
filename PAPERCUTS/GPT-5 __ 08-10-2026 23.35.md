# Papercuts

- An isolated deployment assertion required the same semantic-sufficiency
  marker in all four changed skills, but the reviewer intentionally uses
  asymmetric verdict wording. The installer had succeeded; deployment checks
  need role-specific stable fragments.
