# Papercut

- При поиске исторической правки был использован неполный commit hash, из-за чего `git show` сначала вернул `ambiguous argument`; после уточнения hash исследование продолжилось без влияния на рабочие файлы.
- Первый deployed `mb-doctor` smoke был запущен из source-repo, а не из временного target; ошибка выглядела как отсутствие generated Memory Bank. Повторный запуск с `cwd` target прошёл корректно.
