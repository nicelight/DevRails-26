# Papercuts

- A combined evidence command used the target-project working directory for canonical DevRails paths, producing avoidable `No such file or directory` noise before the paths were rerun from the source repository.
- An `rg` pattern containing Markdown backticks was passed through a double-quoted shell command, so command substitution produced avoidable shell errors and noisy output.
