# Papercuts

- A combined read of the mandatory project-map sources exceeded the tool output limit and was truncated, requiring the large installer file to be reread in smaller ranges.
- A broad multi-file patch failed atomically after a concurrently edited workflow section no longer matched its earlier context; the current diff had to be reconciled before applying narrower patches.
