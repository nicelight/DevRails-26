# Papercuts

- A multi-file `apply_patch` failed atomically because one expected route line
  had different wording; split contract edits into smaller file-scoped patches.
- Overlapping `sed` ranges made one line appear duplicated and caused a second
  patch context miss; inspect non-overlapping ranges before deduplication.
- A regression file was refactored by a neighboring session after earlier
  inspection, so a stale large patch context failed; re-read the live file and
  patch its current helpers instead of relying on compacted context.
