# Papercuts

- The shell tool rejected an isolated installer verification command before execution because its cleanup fragment used `rm -f`; rerun without cleanup.
- A runtime install assertion searched a Markdown phrase across a line wrap with fixed-string `rg`; verify wrapped text with separate stable fragments.
