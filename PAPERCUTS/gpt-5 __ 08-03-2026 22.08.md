# Workflow papercuts

- The first combined patch did not apply because one expected Markdown-table line differed from the current file. Splitting the patch into smaller edits resolved it.
- An initial `rg` status scan used a literal `\n` without multiline mode and failed; rerunning with line-oriented patterns worked.
