# Papercuts

- `rg --files --hidden -g AGENTS.md /` did not complete because traversal from
  the filesystem root entered slow system trees. Whole-OS file discovery needs
  an explicit traversal helper that skips virtual mounts such as `/proc`,
  `/sys`, `/dev`, and `/run`.
- A yielded `functions.exec` wait completed while the nested terminal command
  remained active under a separate session ID, requiring an additional poll or
  interrupt through `write_stdin`.
