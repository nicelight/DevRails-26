# Papercuts

- `test-install-sync.mjs` initially matched a long runtime-contract phrase
  across Markdown wrapping; the generated content was correct, but the brittle
  textual assertion failed until it was reduced to a stable semantic marker.
